pipeline{
	agent any

	stages{
		stage('Code Repo'){
			steps{
				git url: "https://github.com/Yatingambhir85/jenkins-demo-task.git", branch: 'main'
			}
		}
		stage('Build'){
		      steps{
			      echo 'Building Image'
			      sh "docker build -t my-practice-image -f firstpipelinedemo/Dockerfile ."
		      }
		}
		stage('Deploy'){
			steps{
				echo 'Deploying the container'
				sh "docker run -itd --name my-practice-container -p 8081:80 my-practice-image"
			}
		}
	}
}
